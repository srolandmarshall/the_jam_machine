%h1 The Jam Machine

= form_with url: jam_path, method: :post, local: true do |f|
  = f.label :input, 'Enter your request:'
  %br/
  = f.text_area :input, rows: 4, cols: 50
  %br/
  = f.submit 'Generate music'
